from setuptools import setup, find_packages

setup(
    name='engarde',
    version='0.0.1',
    url='https://github.com/vanonselenp/engarde',
    description='Random remake reskin of existing website',
    install_requires=[
        'markdown',
        'flask',
    ],
)
